---
import BaseLayout from '../../layouts/BaseLayout.astro';
import PortableText from '../../components/PortableText.astro';
import { t } from '../../lib/translations';
import { getStaticPage } from '../../lib/sanity';

const lang = 'fr';

// Try to fetch contact page content from Sanity
const contactContent = await getStaticPage('contact', lang);
---

<BaseLayout title="Contactez-Nous - Colourfully Digital" currentLang={lang}>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    {contactContent ? (
      <div>
        <h1 class="text-4xl font-bold text-gray-900 mb-8">
          {contactContent.pageTitle}
        </h1>
        <div class="prose prose-lg max-w-none">
          <PortableText content={contactContent.pageContent} />
        </div>
      </div>
    ) : (
      <div>
        <h1 class="text-4xl font-bold text-gray-900 mb-8">
          Contactez-Nous
        </h1>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-12">
          <div>
            <h2 class="text-2xl font-bold text-gray-900 mb-4">Entrez en Contact</h2>
            <p class="text-gray-600 mb-6">
              Nous aimerions avoir de vos nouvelles ! Que vous souhaitiez devenir partenaire, faire du bénévolat ou en savoir plus sur notre travail, nous sommes là pour vous aider.
            </p>
            <div class="space-y-4">
              <div>
                <h3 class="font-semibold text-gray-900">Demandes Générales</h3>
                <p class="text-gray-600">info@colourfullydigital.org</p>
              </div>
              <div>
                <h3 class="font-semibold text-gray-900">Partenariats</h3>
                <p class="text-gray-600">partnerships@colourfullydigital.org</p>
              </div>
              <div>
                <h3 class="font-semibold text-gray-900">Opportunités de Bénévolat</h3>
                <p class="text-gray-600">volunteer@colourfullydigital.org</p>
              </div>
            </div>
          </div>
          <div>
            <h2 class="text-2xl font-bold text-gray-900 mb-4">Envoyez-nous un Message</h2>
            <form class="space-y-4">
              <div>
                <label for="name" class="block text-sm font-medium text-gray-700 mb-1">Nom</label>
                <input type="text" id="name" name="name" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" required />
              </div>
              <div>
                <label for="email" class="block text-sm font-medium text-gray-700 mb-1">Email</label>
                <input type="email" id="email" name="email" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" required />
              </div>
              <div>
                <label for="subject" class="block text-sm font-medium text-gray-700 mb-1">Sujet</label>
                <input type="text" id="subject" name="subject" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" required />
              </div>
              <div>
                <label for="message" class="block text-sm font-medium text-gray-700 mb-1">Message</label>
                <textarea id="message" name="message" rows="4" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" required></textarea>
              </div>
              <button type="submit" class="w-full bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700 transition-colors">
                Envoyer le Message
              </button>
            </form>
          </div>
        </div>
      </div>
    )}
  </div>
</BaseLayout>
